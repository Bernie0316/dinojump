遊戲分解(
    邏輯：讓恐龍跳起來躲過障礙物的遊戲
    樣式。：畫面不動、角色移動
    角色們：恐龍
        物件：樹、雲、太陽、背景、變數(里程數(變數))
)

程式設計(
    恐龍：
        跳躍
        奔跑、撞樹動作，(恐龍會什麼時候停止奔跑呢？)直到碰到樹
        奔跑距離計算、回報，直到碰到樹
    樹：
        反覆從畫面最右邊移動到畫面最左邊，直到碰到恐龍
    雲、太陽：
        反覆從畫面最右邊移動到畫面最左邊，直到樹碰到恐龍(訊息)
    背景：
        一張背景圖，圖片不動
)